{
  "title": "{{.ServiceName}}",
  "uid": "{{.ServiceName}}",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "10s",
  "panels": [
    {"type":"timeseries","title":"Requests per second","gridPos":{"h":8,"w":8,"x":0,"y":0},"targets":[{"expr":"sum(rate(http_requests_total{service=\"{{.ServiceName}}\"}[5m]))","legendFormat":"requests/s"}]},
    {"type":"timeseries","title":"5xx per second","gridPos":{"h":8,"w":8,"x":8,"y":0},"targets":[{"expr":"sum(rate(http_requests_total{service=\"{{.ServiceName}}\",status=~\"5..\"}[5m]))","legendFormat":"5xx/s"}]},
    {"type":"timeseries","title":"p95 latency","gridPos":{"h":8,"w":8,"x":16,"y":0},"targets":[{"expr":"histogram_quantile(0.95, sum by (le) (rate(http_request_duration_seconds_bucket{service=\"{{.ServiceName}}\"}[5m])))","legendFormat":"p95"}]}
  ]
}
